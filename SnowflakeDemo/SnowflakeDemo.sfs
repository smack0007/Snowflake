var temp = (func(var x) { return x; })(5);
print(temp);

var x = 5;
x / 5;

var buildAdder = func(var x) {
	return func(var y) {
		return x + y;
	};
};

var adder = buildAdder(5);

while (x != 8) {
	print("Hello World!");
	x += 1;
}

for (var y = 0; y != 10; y += 1) {
	print(y);
}

print("x = " + x);
print("y = " + y);
